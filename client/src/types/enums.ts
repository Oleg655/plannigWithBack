export const enum SidebarPage {
  STAGES,
  PARAMETERS,
}

export const enum ResizeMode {
  NEXTCOLUMN,
  SCROLL,
}

export const enum PATHS {
  LOGIN = './login',
  TABLE = './table',
  NOT_FOUND = '/*',
}
